// Background service code removed.
// This stub remains only to avoid broken imports if any file still references initializeService.
// Safe no-op implementations:

Future<void> initializeService() async {
  // No background service needed.
}
